# This file was generated by liblab | https://liblab.com/

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "nyt-archives"
version = "1.0.0"
license = { text = "MIT" }
description = """Welcome to the NytArchives SDK documentation. This guide will help you get started with integrating and using the NytArchives SDK in your project."""
readme = "README.md"
requires-python = ">=3.7"
dependencies = [
    "requests>=2.31.0"
]
